use std::{collections::HashMap, marker::PhantomData};
use super::ResourceLocation;

// static mut BLOCK_REGISTER_TABLE: HashMap<ResourceLocation,PhantomData<i32>> = HashMap::new().insert(ResourceLocation::new("blockworld:air"),);

